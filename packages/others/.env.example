# =============================================================================
# Variables d'environnement - Feature 001 : keycloak-docker-compose-setup
# =============================================================================
# Ce fichier contient toutes les variables d'environnement nécessaires pour
# les services Docker Compose (Keycloak, PostgreSQL, Mailpit).
# 
# Note : Les valeurs par défaut sont appropriées pour un environnement de
# développement/POC. En production, utilisez des mots de passe sécurisés et
# configurez HTTPS.
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL / Base de données
# -----------------------------------------------------------------------------
# Configuration de la base de données PostgreSQL utilisée par Keycloak

# Nom de la base de données PostgreSQL
POSTGRES_DB=keycloak

# Utilisateur PostgreSQL
POSTGRES_USER=keycloak

# Mot de passe PostgreSQL (à changer en production)
POSTGRES_PASSWORD=keycloak

# -----------------------------------------------------------------------------
# Keycloak - Connexion à la base de données
# -----------------------------------------------------------------------------
# Variables de connexion de Keycloak à PostgreSQL

# Type de base de données (postgres, mysql, mariadb, etc.)
KC_DB=postgres

# Nom du service PostgreSQL dans docker-compose (nom du service, pas l'hostname)
KC_DB_URL_HOST=db

# Nom de la base de données
KC_DB_URL_DATABASE=keycloak

# Utilisateur PostgreSQL pour Keycloak
KC_DB_USERNAME=keycloak

# Mot de passe PostgreSQL pour Keycloak
KC_DB_PASSWORD=keycloak

# Port PostgreSQL
KC_DB_URL_PORT=5432

# -----------------------------------------------------------------------------
# Keycloak - Configuration générale
# -----------------------------------------------------------------------------
# Variables de configuration Keycloak pour le développement

# Hostname de Keycloak (pour le développement, utilisez localhost)
# KC_HOSTNAME=localhost

# Activer HTTP pour le développement (true pour dev, false pour production)
KC_HTTP_ENABLED=true

# Désactiver la vérification stricte du hostname (pour le développement)
KC_HOSTNAME_STRICT=false

# Désactiver la vérification HTTPS stricte (pour le développement)
KC_HOSTNAME_STRICT_HTTPS=false

# -----------------------------------------------------------------------------
# Keycloak - Administration
# -----------------------------------------------------------------------------
# Configuration de l'utilisateur administrateur Keycloak
# Ces variables fonctionnent avec la commande 'start' (pas 'start-dev')

# Nom d'utilisateur de l'administrateur Keycloak
KC_BOOTSTRAP_ADMIN_USERNAME=admin

# Mot de passe de l'administrateur Keycloak (à changer en production)
KC_BOOTSTRAP_ADMIN_PASSWORD=admin

# -----------------------------------------------------------------------------
# oauth2-proxy
# -----------------------------------------------------------------------------
# Configuration du proxy oauth2
# Uniquement les secrets

# Client secret du client OAuth2 (client confidentiel)
OAUTH2_PROXY_CLIENT_SECRET=NDJepSnDfWPKJSo8cbe9bXhrB11VYvFQ

# OAuth2 Proxy - Configuration minimale pour démarrage
OAUTH2_PROXY_COOKIE_SECRET=vCzi9LdkI7x-TOCnNGjkBjCmT2wKY25iYKTZzu_8Tx8=
